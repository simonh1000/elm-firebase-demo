{
  "rules": {
    ".read": "auth != null && root.child('emails').hasChild(auth.token.email.replace('@','_').replace('.','_'))",
    ".write": "auth != null && root.child('emails').hasChild(auth.token.email.replace('@','_').replace('.','_'))"
  }
}
